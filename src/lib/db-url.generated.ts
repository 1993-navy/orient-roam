// Placeholder — overwritten at build time by scripts/netlify-db-setup.mjs.
// Committed only so TypeScript resolves the import; the real value (the Neon
// URL) is injected during the Netlify build and is git-ignored thereafter.
export const DATABASE_URL: string | undefined = undefined;
